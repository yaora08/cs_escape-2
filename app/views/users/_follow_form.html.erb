 <% unless current_user?(@user) %> <%#自分自身のプロフィールページなら表示しない %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>