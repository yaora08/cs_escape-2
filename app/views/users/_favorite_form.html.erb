<div id="favorite-<%= @micropost.id %>">
  <% if !current_user.nil? && current_user.favorite?(@micropost) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
